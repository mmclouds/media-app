export type VideoGeneratorAsset = {
  id: string;
  title: string;
  duration: string;
  resolution: string;
  src: string;
  poster: string;
  tags: string[];
};

export type VideoGeneratorHistory = string[];

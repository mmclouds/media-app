## 上下文
- 目标页面：`/media-studio/nano-banana` 内页
- 目标：补充 SEO 友好内容并提升模型理解度与营销转化
- 约束：不引入新字体；不修改落地页/示例页 SEO 属性；文案接入多语言机制；保留 `MediaGeneratorWorkspace`

## 版式草图（基于 ui-ux-pro-max 输出）
**结构模式：Hero + Features + CTA**

1. Hero
   - H1: Nano Banana Studio
   - 副标题：一句话解释模型价值（多语言）
   - 快速要点：3 个短标签（速度、风格一致性、可控编辑）（多语言）
   - Primary CTA：Start generating（多语言）

2. Model Snapshot
   - 简要介绍（2-3 句，多语言）
   - 关键能力卡片 3-4 个（text-to-image / image-to-image / style control / batch-ready）

3. Workflow
   - 4 步流程：Choose mode → Craft prompt → Preview → Iterate & export（多语言）

4. Use Cases
   - 4-6 个场景：Product mockups / Social posts / Storyboarding / Concept art / Fast edits（多语言）

5. Prompt Tips
   - 提示写作结构（Subject + Style + Lighting + Lens，多语言）
   - 2-3 条示例（多语言）

6. FAQ
   - 3-5 条高频问题（分辨率、编辑输入、成本、输出格式，多语言）

7. CTA
   - 复述价值 + 行动按钮（多语言）

## 视觉与风格建议
- 风格：Vibrant & Block-based（延续现有深色背景，加入块状分区与对比强调）
- 配色：保持当前深色主背景，局部使用高对比强调色做 CTA
- 文字：沿用站点现有字体，避免在页面层级引入新字体
- 动效：轻量渐入/分段 reveal，注意 `prefers-reduced-motion`

## 可访问性与 SEO
- 使用语义化标题层级（H1 仅一次，H2/H3 分区）
- 关键内容段落保持 1-2 句短段，避免长墙文本
- CTA 按钮具备可聚焦样式与明确文案
- 文案全部走国际化键值，确保语言切换一致

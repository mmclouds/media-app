## 1. 实施
- [x] 1.1 在 `page.tsx` 扩展结构化内容区块（Hero、Features、Workflow、Use Cases、FAQ、CTA），保留 `MediaGeneratorWorkspace`。
- [x] 1.2 接入多语言文案（参考落地页的国际化方式），避免硬编码文本。
- [x] 1.3 强化营销转化向的文案与 CTA 表达，保持 SEO 语义层级清晰。
- [x] 1.4 复用现有样式体系，确保深色背景与对比度符合可读性要求。
- [ ] 1.5 手动检查页面在 375/768/1024 宽度下布局与可读性。

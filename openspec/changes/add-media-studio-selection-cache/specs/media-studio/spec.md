## 新增需求
### 需求：Media Studio 选择缓存
系统应在 Media Studio 页面缓存用户选择的媒体类型与模型，并在刷新后恢复。

#### 场景：刷新后恢复选择
- **当** 用户在 `/media-studio` 选择媒体类型与模型
- **并且** 刷新页面
- **那么** 页面恢复到上次选择的媒体类型
- **并且** 默认模型为上次选择的模型（若仍可用）

#### 场景：缓存模型不可用
- **当** 用户缓存的模型不属于当前媒体类型
- **那么** 系统回退到当前媒体类型的首个可用模型
